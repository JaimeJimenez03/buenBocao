package com.buenbocao.api.common.enums;

/**
 * Tipo de cuenta del usuario.
 * Separado del Role — el rol gestiona permisos, el tipo de cuenta gestiona funcionalidades.
 */
public enum AccountType {
    FREE,
    PREMIUM
}
